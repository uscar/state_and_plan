#include "NaiveActionScorer.h"

namespace planning {
    double NaiveActionScorer::score(const Action& action) {
        // TODO run heuristic for all (or some)  worlds and average out
        
        return 0.0;
    }
}
